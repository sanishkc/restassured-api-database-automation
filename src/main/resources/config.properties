# Base URL for the API under test.
# If the environment variable BASE_URL is set (e.g., in CI/CD), it will use that value.
# Otherwise, it defaults to the Render service URL: https://springboot-api-database-framework-1.onrender.com/
base.url=${BASE_URL:https://springboot-api-database-framework-1.onrender.com/}